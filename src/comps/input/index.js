import React from 'react';
import { View } from 'react-native';
import { styles } from './styles';

const Input = (props) => {
    const {title} = props;
    return (
        <View>
            <Text>This is Input of screen</Text>
        </View>
    );
}

export default Input;