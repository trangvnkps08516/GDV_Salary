import React from 'react';
import { View, Text } from 'react-native';
import { styles } from './styles';

const DatePicker = (props) => {
    const {title} = props;
    return (
        <View>
            <Text>This is DatePicker of screen</Text>
        </View>
    );
}

export default DatePicker;